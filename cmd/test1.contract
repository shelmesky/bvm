contract testDefS {

    func myfunc1(str a) str {   // a: 0
        arr.str as              // as: 1
        as += `aaa`
        return ""
    }



    func myfunc2(str b) str {   // b: 2
        arr.str bs              // bs: 3
        bs += `aaa`
        return ""
    }

    myfunc1("xxx")

    arr.str bbb
    bbb += `bbb`
    println(bbb[0])

    return
}