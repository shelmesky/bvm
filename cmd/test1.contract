contract testDefS {

    // 全局变量声明放在代码最开始

    func myfunc1(str aaa, str bbb) str {
        arr.int myintarr
        myintarr += 112
        return aaa + bbb
    }

     func myfunction(str a) str {
                arr.str myarr1
                myarr1 += `abcdefg`
                return a + `...`
     }

    myfunction("gogogo")
    myfunc1("xxxx", "yyyy")


    return
}
