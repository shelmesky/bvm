contract testDefS {

    func myfunc1(str a) str {   // a: 0
        arr.str as              // as: 1
        as += `aaa`
        return ""
    }



    func myfunc2(str b) str {   // b: 2
        arr.str bs              // bs: 3
        bs += `aaa`
        return ""
    }

    myfunc1("aaa")
    myfunc2("bbb")

    arr.str bbb
    bbb += `bbb`
    println(bbb[0])

    func get() map.str {
        map.str mapb
        mapb[`0`] = `q`
        mapb[`1`] = `w`
        return mapb
    }

    map.str aaa = get()

    return
}