contract testDefS {

    func myfunc1(str a) str {
        arr.str as
        as += `aaa`
        return ""
    }


    func myfunc2(str b) str {
        arr.str bs
        bs += `aaa`
        return ""
    }

    myfunc1("aaa")
    myfunc2("bbb")

    return
}