contract testDefS {

    func myfunc1(str a) str {   // a: 0
        arr.str as              // as: 1
        as += `aaa`
        return "this myfunc1..."
    }

    str xxx = myfunc1("xxx")
    println(xxx)

    arr.str bbb
    bbb += `bbb`
    println(bbb[0])

    return
}
