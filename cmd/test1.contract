contract testDefS {
    data {
        str s1
        int a1
        int b2
        str b3
    }

    // 全局变量声明放在代码最开始
    arr.str ai
    arr.int aaa

    func myfunction(str a) str {
            arr.str myarr1
            myarr1 += `abcdefg`
            return a + `...`
    }

    myfunction("gogogo")

    ai += `hahaha`

    return
}
